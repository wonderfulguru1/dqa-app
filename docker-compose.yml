services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - '5004:5004'
    env_file:
      - .env
    environment:
      PORT: '5004'
      COOKIE_SECURE: 'false'
      SEED_EMAIL: admin@ecews.org
      SEED_PASSWORD: DQA2025!
      SEED_NAME: DQA Admin
    extra_hosts:
      - 'host.docker.internal:host-gateway'
